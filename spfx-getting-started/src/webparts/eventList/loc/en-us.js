define([], function() {
  return {
    "PropertyPaneDescription": "Here you can configure the settings of the events lilst webpart. Check if the list name property is correct, otherwise the webpart won't work.",
    "BasicGroupName": "Basic configuration",
    "ListNameLabel": "List name",
    "ListNameDescription": "Name of the list that contains the event data. (e.g. Events)",
    "ListNamePlaceholder": "Events"
  }
});
